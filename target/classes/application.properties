# Server Configuration
server.port=9091
server.servlet.context-path=/api/v1

# Application Configuration
spring.application.name=aigreentick-notification

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=notificationdb
spring.data.mongodb.authentication-database=admin
spring.data.mongodb.username=
spring.data.mongodb.password=

# MongoDB Connection String (alternative configuration)
# spring.data.mongodb.uri=mongodb://localhost:27017/notificationdb

# MongoDB Logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Mail Configuration (configure with your SMTP settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:jahanvinwork@gmail.com}
spring.mail.password=${MAIL_PASSWORD:kjjx rihw gllb nalb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Firebase Configuration
firebase.service-account-key=firebase-service-account.json
firebase.project-id=aigreentick-40943

# Logging Configuration
logging.level.com.aigreentick.notification=DEBUG
logging.level.org.springframework.web=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
